{
	"name": "shopping",
	"sourceRoot": "libs/shopping",
	"projectType": "library",
	"targets": {
		"migrate-create": {
			"executor": "nx:run-commands",
			"options": {
				"cwd": "libs/shopping",
				"command": "migrate create -seq -ext sql -dir infrastructure/db/migrations {args.name}"
			}
		},
		"migrate-up": {
			"executor": "nx:run-commands",
			"options": {
				"cwd": "libs/shopping",
				"command": "migrate -database 'postgres://postgres:postgres@localhost:5432/kerabu?sslmode=disable&x-migrations-table=shopping_schema_migrations' -path infrastructure/db/migrations up"
			}
		},
		"migrate-down": {
			"executor": "nx:run-commands",
			"options": {
				"cwd": "libs/shopping",
				"command": "migrate -database 'postgres://postgres:postgres@localhost:5432/kerabu?sslmode=disable&x-migrations-table=shopping_schema_migrations' -path infrastructure/db/migrations down"
			}
		}
	}
}
