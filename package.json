{
  "name": "workshop",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "nx run-many --target=build",
    "dev": "nx run portfolio:dev",
    "preview": "nx run portfolio:preview",
    "build:portfolio": "nx run portfolio:build"
  },
  "private": true,
  "packageManager": "bun@1.3.12",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "nx": "^22.6.5"
  }
}
